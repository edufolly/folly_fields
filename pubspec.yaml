name: folly_fields
description: Basic form fields and utilities.
version: 0.0.1
#author:
#homepage:

environment:
  sdk: ">=2.8.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
